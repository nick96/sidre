<Assertion xmlns="urn:oasis:names:tc:SAML:2.0:assertion" xmlns:samlp="urn:oasis:names:tc:SAML:2.0:protocol" ID="{{ assertion_id }}" IssueInstant="{{ issue_instant }}" Version="2.0">
    <Issuer>{{ issuer }}</Issuer>
    <Subject>
        <NameID Format="{{ name_id_format }}">{{ name_id }}</NameID>
        <SubjectConfirmation Method="urn:oasis:names:tc:SAML:2.0:cm:bearer">
            <SubjectConfirmationData InResponseTo="{{ response_id }}" NotOnOrAfter="{{ not_on_or_after }}" Recipient="{{ consume_endpoint }}" />
        </SubjectConfirmation>
    </Subject>
    <Conditions NotBefore="{{ not_before }}" NotOnOrAfter="{{ not_on_or_after }}">
        <AudienceRestriction>
            <Audience>{{ sp_entity_id }}</Audience>
        </AudienceRestriction>
    </Conditions>
    <AuthnStatement AuthnInstant="{{ issue_instant }}">
        <AuthnContext>
            <AuthnContextClassRef>urn:oasis:names:tc:SAML:2.0:ac:classes:PasswordProtectedTransport</AuthnContextClassRef>
        </AuthnContext>
    </AuthnStatement>
</Assertion>